#
# path: /crates/engine_scene/Cargo.toml
# description: Cargo manifest for the scene management crate of the engine.
#

[package]
name = "engine_scene"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
ron = "0.8"

