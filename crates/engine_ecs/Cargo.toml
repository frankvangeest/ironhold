#
# path: /crates/engine_ecs/Cargo.toml
# description: Cargo manifest for the engine entity-component-system crate
#

[package]
name = "engine_ecs"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_ecs = "0.14"
serde = { version = "1", features = ["derive"] }
