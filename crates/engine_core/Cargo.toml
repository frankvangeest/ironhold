#
# path: /crates/engine_core/Cargo.toml
# description: Cargo manifest for the engine_core crate
#

[package]
name = "engine_core"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_ecs = "0.14"
thiserror = "1"
