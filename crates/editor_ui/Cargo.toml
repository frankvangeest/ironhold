#
# path: /crates/editor_ui/Cargo.toml
# description: Cargo manifest for the editor_ui crate
#

[package]
name = "editor_ui"
version = "0.1.0"
edition = "2021"

[dependencies]
egui = "0.29"
engine_core = { path = "../engine_core" }
# wgpu = "27.0"
# thiserror = "1"
