#
# path: /crates/engine_ui/Cargo.toml
# description: Cargo manifest for the UI crate of the engine.
#

[package]
name = "engine_ui"
version = "0.1.0"
edition = "2021"

[dependencies]
taffy = "0.5"
serde = { version = "1", features = ["derive"] }
