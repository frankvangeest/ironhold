#
# path: /crates/editor_core/Cargo.toml
# description: Cargo manifest for the editor_core crate
#

[package]
name = "editor_core"
version = "0.1.0"
edition = "2021"

[dependencies]
engine_core = { path = "../engine_core" }
bevy_ecs = "0.14"
thiserror = "1"

