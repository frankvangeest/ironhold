#
# path: /crates/engine_render/Cargo.toml
# description: Cargo manifest for the engine_render crate
#

[package]
name = "engine_render"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "0.20"
thiserror = "1"
