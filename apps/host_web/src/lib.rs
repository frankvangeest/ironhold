/**
 * path: /apps/host_web/src/lib.rs
 * description: Web host application for the engine WASM module.
 */

use wasm_bindgen::prelude::*;

#[wasm_bindgen(start)]
pub fn start() {
    // In practice you'd import the npm engine and mount it onto a canvas.
}
